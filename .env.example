VITE_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
VITE_SUPABASE_ANON_KEY=YOUR_ANON_KEY

# Only needed for one-off ingest scripts (e.g. scripts/ingest-pdfs.mjs).
# Get it from Supabase → Settings → API → Project API keys → service_role.
# NEVER commit this. NEVER ship it to the browser. The frontend uses the anon key only.
SUPABASE_SERVICE_ROLE_KEY=eyJ...
